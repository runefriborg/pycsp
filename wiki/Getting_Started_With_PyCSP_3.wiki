#summary Tutorial on how to get started with PyCSP (page 3)
#labels Tutorial
#sidebar TutorialList

= Termination =

{{{
from pycsp.parallel import pycsp

C = pycsp.Channel()
@pycsp.process
def P(cin):
  while True:
    val = cin()
    ...

pycsp.Spawn(P(C.reader())
}}}

Shutdown on next read.

{{{
  pycsp.poison(cin)
}}}

Exception handling:
{{{
from pycsp.parallel import pycsp

@pycsp.process
def streamFile(cout, filename):
  try:
    f = open(filename)
    lock(f)
    ...
  except pycsp.ChannelPoisonException:
    unlock(f)
    f.close()
}}}
